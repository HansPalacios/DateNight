<header>
	<div class="container-fluid">
		<div class="row">
			<div class="col-md-12">
			<%= link_to '/' do h1_tag "Date Night" end %>
       	<div id="links">
				<% if user_signed_in? %>
  						<%= link_to "Sign Out", destroy_user_session_path, method: :delete, :id => 'signout' %>
  					<% else %>
         	<%= link_to 'Sign Up', '#', :id => 'signup' %>
	 				<%= link_to 'Sign In', '#', :id => 'signin' %>
 				<% end %>
 			</div>
		</div>		
	</div>
</header>